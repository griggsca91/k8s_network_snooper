# Network Snooper

Using https://github.com/aya-rs/aya create a custom operator that injects this into the node and monitors network requests to create a flow diagram of communication

Ideally it should be a simple operator that gets installed and starts collecting the metrics and details that it can forward to some metrics ingester

